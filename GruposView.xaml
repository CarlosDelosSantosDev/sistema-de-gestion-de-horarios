<UserControl x:Class="SistemaHorarios.GruposView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Height="Auto" Width="Auto">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Margin="0,0,0,20">
            <TextBlock Text="Gestión de Grupos" 
                     FontSize="20" FontWeight="Bold" 
                     Foreground="#FF1E3A5F"/>
            <TextBlock Text="Administra los grupos del sistema" 
                     Foreground="#FF546E7A" FontSize="14"/>
        </StackPanel>

        <!-- Botones de Acción -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,0,0,20">
            <Button x:Name="btnAgregar" Content="➕ Agregar Grupo" 
                  Background="#FF1E3A5F" Foreground="White" 
                  Padding="15,8" Margin="0,0,10,0"
                  Click="BtnAgregar_Click"/>
            <Button x:Name="btnRefrescar" Content="🔄 Refrescar" 
                  Background="#FF546E7A" Foreground="White" 
                  Padding="15,8"
                  Click="BtnRefrescar_Click"/>
        </StackPanel>

        <!-- DataGrid -->
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto"
                    Margin="0,0,0,20">
            <DataGrid x:Name="dgGrupos" 
                    AutoGenerateColumns="False" CanUserAddRows="False"
                    IsReadOnly="True" SelectionMode="Single" 
                    SelectionUnit="FullRow" MinHeight="300">

                <DataGrid.Columns>
                    <DataGridTextColumn Header="ID" Binding="{Binding IdGrupo}" Width="150"/>
                    <DataGridTextColumn Header="Grado" Binding="{Binding Grado}" Width="150"/>
                    <DataGridTextColumn Header="Sección" Binding="{Binding Seccion}" Width="150"/>
                    <DataGridTextColumn Header="Carrera" Binding="{Binding NombreCarrera}" Width="480"/>
                    <DataGridTextColumn Header="Turno" Binding="{Binding NombreTurno}" Width="150"/>

                    <!-- ✅ SOLO AGREGAR ESTA COLUMNA NUEVA -->
                    <DataGridTextColumn Header="Modalidad" Binding="{Binding Modalidad}" Width="150"/>

                    <!-- Columna de Acciones -->
                    <DataGridTemplateColumn Header="Acciones" Width="100">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal">
                                    <Button Content="✏️" Margin="2" Padding="8,5" 
                                          Click="BtnEditar_Click" 
                                          Tag="{Binding IdGrupo}"
                                          ToolTip="Editar grupo"
                                          Background="Transparent" BorderThickness="0"/>
                                    <Button Content="🗑️" Margin="2" Padding="8,5" 
                                          Click="BtnEliminar_Click" 
                                          Tag="{Binding IdGrupo}"
                                          ToolTip="Eliminar carrera"
                                          Background="Transparent" BorderThickness="0"/>
                                </StackPanel>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>
        </ScrollViewer>

        <StatusBar Grid.Row="2" Background="#FFF5F5F5">
            <TextBlock x:Name="txtStatus" Text="Listo" Foreground="#FF546E7A"/>
        </StatusBar>
    </Grid>
</UserControl>